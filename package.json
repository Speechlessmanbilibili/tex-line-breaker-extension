{
  "name": "tex-line-breaker-extension",
  "version": "0.3.3",
  "private": true,
  "scripts": {
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown -p tex-line-breaker-core && node scripts/copy-wasm.mjs",
    "test": "cargo test --workspace && node --test tests/*.test.mjs"
  }
}
